apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "stock.fullname" . }}-config
  labels:
    {{- include "stock.selectorLabels" . | nindent 4 }}
data:
  MONGODB_URI: {{ .Values.env.MONGODB_URI | quote }}
  PORT: {{ .Values.env.PORT | quote }}