apiVersion: v1
kind: Service
metadata:
  name: {{ include "stock.fullname" . }}-preview
  labels:
    {{- include "stock.selectorLabels" . | nindent 4 }}
    role: preview
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.env.PORT }}
      protocol: TCP
      name: http
  selector:
    {{- include "stock.selectorLabels" . | nindent 4 }}
