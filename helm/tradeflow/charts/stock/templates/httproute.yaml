apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "stock.fullname" . }}-route
  labels:
    app.kubernetes.io/part-of: {{ include "stock.fullname" . }}
    environment: {{ .Values.global.environment }}
spec:
  parentRefs:
    - name: {{ .Release.Namespace }}-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/stocks
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /api
      backendRefs:
        - name: {{ include "stock.fullname" . }}
          port: {{ .Values.service.port }}
    - matches:
        - path:
            type: PathPrefix
            value: /health
      backendRefs:
        - name: {{ include "stock.fullname" . }}
          port: {{ .Values.service.port }}