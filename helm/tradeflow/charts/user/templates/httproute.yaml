apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "user.fullname" . }}-route
  labels:
    app.kubernetes.io/part-of: {{ include "user.fullname" . }}
    environment: {{ .Values.global.environment }}
spec:
  parentRefs:
    - name: {{ include "tradeflow.fullname" . }}-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/users
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /api
      backendRefs:
        - name: {{ include "user.fullname" . }}
          port: {{ .Values.service.port }}
    - matches:
        - path:
            type: PathPrefix
            value: /health
      backendRefs:
        - name: {{ include "user.fullname" . }}
          port: {{ .Values.service.port }}