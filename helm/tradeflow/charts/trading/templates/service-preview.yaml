apiVersion: v1
kind: Service
metadata:
  name: {{ include "trading.fullname" . }}-preview
  labels:
    {{- include "trading.selectorLabels" . | nindent 4 }}
    role: preview
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.env.PORT }}
      protocol: TCP
      name: http
  selector:
    {{- include "trading.selectorLabels" . | nindent 4 }}
