apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "portfolio.fullname" . }}-route
  labels:
    app.kubernetes.io/part-of: {{ include "portfolio.fullname" . }}
    environment: {{ .Values.global.environment }}
spec:
  parentRefs:
    - name: {{ .Release.Namespace }}-gateway
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/portfolio
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /api
      backendRefs:
        - name: {{ include "portfolio.fullname" . }}
          port: {{ .Values.service.port }}
    - matches:
        - path:
            type: PathPrefix
            value: /health
      backendRefs:
        - name: {{ include "portfolio.fullname" . }}
          port: {{ .Values.service.port }}