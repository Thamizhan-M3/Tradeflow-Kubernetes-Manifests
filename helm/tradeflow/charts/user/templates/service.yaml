apiVersion: v1
kind: Service
metadata:
  name: {{ include "user.fullname" . }}
  labels:
    {{- include "user.selectorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.env.PORT | quote }}
      targetPort: {{ .Values.env.PORT | quote }}
      protocol: TCP
      name: http
  selector:
    {{- include "user.selectorLabels" . | nindent 4 }}
